# Local scanner service
HOST="127.0.0.1"
PORT="4000"

# Required unless SCANNER_ALLOW_UNAUTHENTICATED=1 is explicitly set for throwaway local testing.
SCANNER_API_KEY="replace-with-a-local-secret"

# Keep low until Redis/worker queues are added.
SCANNER_CONCURRENCY="1"
SCAN_JOB_TIMEOUT_MS="180000"
