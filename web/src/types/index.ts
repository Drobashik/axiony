export type AccentColor = "blue" | "green" | "violet";

// axe-core / WCAG impact levels, shared across the scan + dashboard UIs.
export type Severity = "critical" | "serious" | "moderate" | "minor";

export type IconName =
  | "scan"
  | "check"
  | "arrow"
  | "terminal"
  | "ci"
  | "report"
  | "team"
  | "bolt"
  | "code"
  | "selector"
  | "globe"
  | "json";
