/**
 * @deprecated Import directly from `classnames`:
 *
 * ```ts
 * import cn from "classnames";
 * ```
 *
 * This module is kept only as a thin re-export to avoid breaking any
 * stragglers that might still import from `@/lib/cn`. Safe to delete
 * once nothing references it anymore.
 */
export { default, default as cn } from "classnames";
